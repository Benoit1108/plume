# openapi-typescript déclare un peer typescript@^5 (plage non mise à jour pour TS 6, aucune
# incompat réelle — c'est un outil de codegen). On autorise l'install malgré ce peer, de façon
# cohérente en local ET en CI (npm ci).
legacy-peer-deps=true
